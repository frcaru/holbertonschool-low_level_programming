#include "main.h"

/**
 * times_table - func to prints the last digit
 *@c : input
 *
 * Return: 0
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
}
	else
	{
		return (0);
	}
}
